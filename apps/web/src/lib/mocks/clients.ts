/**
 * Client mocks and types
 * Re-exports types from @/types/client for compatibility with legacy components
 */

export type { Client, ClientListItem as ClientListResponse } from "@/types/client";
export { ClientStatus } from "@/types/client";
